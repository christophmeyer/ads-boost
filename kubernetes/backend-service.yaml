apiVersion: v1
kind: Service
metadata:
  name: ads-boost-backend
  namespace: ads-boost
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
